# godtier audit — 2026-07-05

Six parallel read-only audits (bloat, perf, security, robustness, cross-surface, test/i18n) against the 4 pillars. Ranked, file:line'd. `[x]` = shipped this session, `[ ]` = pending.

## ═══ FINISH-THE-LIST STATUS (end of 2026-07-05) ═══

**SHIPPED + verified (~14 commits, all builds green, 1070 tests, synced to loaded dist):**
security (helix allowlist · gql allowlist · css-injection guard · blocked-emote leak · zero-dep+no-eval gates) · bulletproof (emote-cache crash · yt false-sent · scrollback guard · 2 parity gates · 6 fail-loud fixes · user-notes cross-tab merge · settings-ui refuse-save · rememberRemovedEmote wire-in) · cross-surface (/mute async alias · tooltip yt badges) · minimal (~76 LOC dead code · getFiber DRY · source-zip prune · 102 dead i18n keys) · tests (HELIX_ALLOW · sanitizeColor) · docs.

**DEFERRED — needs a DECISION from you (not safe to guess):**
- ~~Helix-dead~~ **RESOLVED (removed)** — color picker + chat-modes panel + clip creation deleted (Twitch killed Helix for the web token; no GQL path existed). The entire Helix proxy (`helixRequest` + `heatsync-helix` handler + `HELIX_ALLOW`) went with them. Kept `/followers` (GQL), whispers, GQL proxy. ~1040 LOC removed.
- **showCosmetics schema** — registering it adds a 2nd "cosmetics" pill next to the existing `subsystems.cosmetics` toggle (they gate the same DOM). Reconcile vs register-hidden — your call.
- **profile-card vs user-notes dual note systems** — `hs_user_notes` (profile-card.js) vs `hs_user_notes_v1` (user-notes.js) are TWO separate note stores. Real fragmentation; needs a reconcile decision.

**DEFERRED — needs LIVE verification before shipping (had to guess):**
- **Kick Pusher replyTo** — fix drafted but field names (`metadata.original_message`) inferred from the outbound shape, not a captured inbound Pusher payload. Capture one first.
- **char-limit per-platform** — fix drafted (twitch/kick 500, yt 200) but touches the hot input + critical send path, and Kick's real cap isn't verified in-repo.

**NOT ATTEMPTED — genuine FEATURE work (not fixes; need per-platform Chrome verify):**
- kick moderation reflection (P0) — kick bans/timeouts don't reflect in the ext
- youtube second-class surface (P1) — username card, paint minting, native-emote wrapping, send retry/queue

**BANKED — low-severity, safe, just not done (call-out, not dropped):**
- bulletproof lows: vim-buffer clipboard false "copied" · early-inject LRU evicts smallest-not-oldest · cross-follow queue lost-update · chat-injector dedup on undefined id · irc kick-key-cap prune
- perf (SAFE but subtle, deferred): `_filterFlags` cache · filter-rules concat · applyMcMutes early-return · highlight-chain single-pass · fairMerge head-slice

---

## shipped this session
- [x] **Helix proxy → 6-endpoint+method allowlist** (`chrome/early-inject-main.js` `HELIX_ALLOW`) — was full-Helix blast radius on a forged MAIN-world message
- [x] **build gates**: `checkNoRuntimeDeps` + `checkNoDynamicCode` (build.js)
- [x] **SECURITY.md/PRIVACY.md**: corrected false "read-only" claim; per-permission rationale + "what we don't request"
- [x] **dead code** (~76 LOC): deleted `image-compress.js`, `heatsync-open-bits-modal` no-op handler, `hsCheermoteUrl()`, `DEFAULT_CHAT_WIDTH`, `LIVE_FETCH_CONCURRENCY`
- [x] **CSS-injection guard** `content.js:5586` — 4th mention-color site was missing the `COLOR_RE` guard the other 3 have
- [x] **emote-cache crash** `emotes.js:2701` — eviction could delete the active channel then read it → throw; mirrored sibling `if(old!==ch)` guard

## e2e Chrome verification (2026-07-05)
- [x] **Helix + GQL allowlists verified live** on the reloaded new-code bundle, real Twitch channel, adversarial + zero account writes:
  - GQL off-list op → "operation not allowed" ✓; allowed ops (incl. the ext's own 5 live requests) pass ✓
  - Helix off-list endpoint → "endpoint not allowed" ✓; allowed endpoints reach the real fetch ✓
  - method: captured the MAIN-world nonce from the ext's own outgoing requests, forged probes through the real handler. **allowlists are release-ready.**
- [ ] **CONFIRMED: Helix proxy is dead with the web token → multiple features silently broken (NEEDS PRODUCT DECISION).** Twitch deprecated Helix for the web session token (live 404s). Impact:
  - **chat color get/set** (picker, `twitch-api.js:322/336/363`) — BROKEN, no GQL path in repo → needs GQL migration (reverse-eng Twitch's persisted-query hash)
  - **chat modes panel** (`renderChatModes` `twitch-api.js:389/394/446`) — BROKEN + fails SILENTLY (returns null → grid just never appears, no error). broadcaster-id resolve has a drop-in GQL fallback (`resolveTwitchChannelId`); follower-mode has a working GQL op (`setTwitchFollowersMode`) — both unwired. The other 4 modes have no known GQL op.
  - **clip creation** (`POST helix/clips`, `twitch-api.js:2154`) — likely broken, unconfirmed (needs live test)
  - partial fixes are moot alone (the panel's initial GET 404s first). **Decision needed: migrate color/modes to GQL (real R&D) vs remove the dead features.** `HELIX_ALLOW` guard stays correct meanwhile. Pre-existing, not caused by the allowlist.

## security (fort-knox) — PENDING
- [x] **HIGH: generic GQL proxy op allowlist — SHIPPED + e2e-verified** (`early-inject-main.js` `ALLOWED_GQL_OPS`, 10-op closed set). was: auto-caches every GQL hash Twitch's own client fires → forge-able unbounded. now gated; verified live (see e2e section).
- [ ] LOW: `heatsync-hermes-event` (raid/pin/redeem banners) has origin check but no nonce (`main.js:12262`) — forgeable fake banners
- [ ] INFO: try dropping `style-src 'unsafe-inline'` (manifest) — no live sink found, quick CSP-violation check
- [ ] INFO: client SSRF pre-filter in `background.js:6318` `fetch_link_preview` is bypassable (`[::1]` vs `::1`, octal/decimal, 169.254.x) — real guard must be server-side; the client check gives false confidence
- [ ] heads-up: `rawQuery` block makes `twitchGql()` Firefox-CORS fallback (twitch-api.js:2578) dead/always-error — fails safe

## robustness (bulletproof) — PENDING, ranked
- [ ] HIGH: `user-notes.js:68-137` cross-tab full-blob overwrite wipes notes (no onChanged merge, writes before load resolves) — mirror mentions.js. M/care
- [ ] HIGH: `youtube-content.js:153` false "sent" — non-awaitConfirm branch reports ok regardless of drop. S
- [ ] HIGH: `profile-card.js:152` note persist swallowed by empty catch — no feedback, reverts empty. S
- [ ] **BUG (not dead code): `rememberRemovedEmote` (emotes.js:2076) is never called** — the removed-emote-render-fallback feature is silently non-functional (nothing populates it with cap+persist). Wire it to the remove-emote action, or remove the whole machinery. Decide.
- [ ] MED: `emotes.js:1740` block/unblock server sync fails invisibly (unawaited + `.catch(()=>{})`) → per-device divergence. toast on catch
- [ ] MED: `tooltips.js:1565` failed link preview cached as permanent null (no negative-TTL)
- [ ] MED: `content.js:4700` one malformed cache entry aborts whole scrollback restore — per-item try/catch + continue
- [ ] MED: `settings-ui.js:591` corrupt filter-rules read → silent `[]`, next save persists the wipe — refuse-to-save on parse fail
- [ ] MED: `platform-detector.js:39`/`chat-injector.js:316` stale Kick selector (dead `.chat-entry-content`) → silent injection blackout, no warn
- [ ] LOW: `irc.js:501/998` archive relay + kick-history persist `.catch(()=>{})` + no cap on hs_kick_* key count
- [ ] LOW cluster: vim-buffer clipboard false "copied"; early-inject LRU evicts numerically-smallest not oldest; cross-follow queue lost-update; chat-injector dedup on undefined id
- [ ] uncertain (grep): irc.js:888 kick webhook vs Pusher d.id namespace; content.js:4860 backfill order assumption; feed-embed.js:416 single-media render

## cross-surface (godtier) — PENDING, ranked
- [ ] P0: **kick ingests no external mod actions** (Pusher tap reads only ChatMessageEvent; no CLEARCHAT/CLEARMSG equiv) — kick ban shows nothing
- [ ] P0: kick mod actions don't dim target's prior msgs (no `m.cleared` session stamp like twitch)
- [ ] P1: YT native chat — no username click/right-click card (content.js not injected on youtube.com)
- [ ] P1: YT paints never mint (client never scrapes `author-external-channel-id` for `yt_<UCid>`; server supports it)
- [ ] P1: YT native emotes are bare `<img>` — no wrapper/block/add/NSFW-gate
- [ ] P1: YT send — silent fanout failure (toast only, no red-border/retry), no retry/queue
- [ ] P1: **twitch native tab-complete leaks BLOCKED emotes** (content.js emote-bridge doesn't filter; kick+YT do) — small, do soon
- [ ] P2: kick rows show initials not real avatars; kick Pusher hardcodes replyTo:null; kick tab-complete ignores frecency; tooltip fallback omits YT badges; `/mute`+`/unmute` use sync-only alias set (miss server links)
- [ ] P2: `showCosmetics` setting bypasses settings-schema (no lint/reset); char-limit hardcoded 500 all platforms (YT ~200)
- [ ] arch: ordering comparator reimplemented 4× (input.js:3706 + 3 hooks, "keep in lockstep by hand") — extract shared ranker (#12 proves it already drifted)

## minimal (bloat/dup) — PENDING
- [ ] extend `checkUiSyncBlocklistParity` to also diff `estimateSettingSize`/`sanitizeUiSettings` bodies (byte-identical, currently ungated silent-drift)
- [ ] add 4th parity gate for `userKey`/`userSetMatches` (user-key.js ⇄ background.js ⇄ content.js, ungated)
- [ ] DRY `getFiber()` across autocomplete-hook.js + twitch-chat-intercept.js (both MAIN, same entry) — 4 LOC, opportunistic
- [ ] exclude `chrome/multichat-*.js` (~7.3MB) from `buildSourceZip()` — reconstructable from src+build.js
- [ ] delete ~102 dead i18n keys (27% of messages.json) — VERIFY each (tools/patches substring refs caused false positives); `popup_*` block safe

## test coverage (godtier) — PENDING
- [ ] `HELIX_ALLOW` allowlist logic — zero tests on the new security boundary (easy pure: method+path+origin exact match vs bypass)
- [ ] `sanitizeColor` (main.js:8398) — stubbed in every test, real regex+cache never run
- [ ] `processEmotes` core tokenizer (emotes.js:2881) — only 1 narrow test
- [ ] `getUserAliases`/`expandUserAliases` mute/block fan-out — stubbed/zero refs (blocked user could leak on linked platform)
- [ ] i18n: ~20 hardcoded user-facing strings bypass `t()` (settings-ui, tooltips, social, predictions) — localization debt

## perf (minimal/godtier) — PENDING
- [ ] `applyMcMutes()` full-DOM QSA every render (input.js:2327) — early-return when `!mutedUsers.size`. care (verify aliases)
- [ ] `_filterFlags()` 3× getSetting per message (main.js:7367) — cache module-scope. SAFE
- [ ] `evaluateFilterRules` per-message array concat (filter-rules.js:405) — iterate in place. SAFE
- [ ] triple HTML re-tokenization in highlight chain (main.js:7094/7184/7208) — single pass. care
- [ ] `fairMerge` filters full source copies per render (main.js:7622) — merge head slices. care
